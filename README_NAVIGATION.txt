NOX-IA — CORRECTION BARRE DE NAVIGATION

Correction :
- suppression du défilement horizontal de la barre de menu ;
- les onglets passent automatiquement à la ligne si l'écran n'est pas assez large ;
- la barre ne reste plus décalée vers la droite entre les pages.

GitHub :
remplace uniquement web_app.py puis Commit changes.
Render redéploiera automatiquement.
